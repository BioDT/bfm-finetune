training:
  precision_in: high
  batch_size: 2
  epochs: 40
  lr: 1e-4
  checkpoint_path: #provide the folder of the weights
  val_every: 5

dataset:
  toy: False
  num_species: 500
  num_workers: 15

model:
  hidden_dim: 160
  base_small: False
  big: True
  big_ft: False